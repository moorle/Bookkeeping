<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/background_light"
    android:orientation="vertical">

    <com.moorle.bookkeeping.ui.widget.MessageCardView
        android:id="@+id/live_now_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:messageText="@string/session_live_now_message"
        app:button1text="@string/no_thanks"
        app:button2text="@string/watch_now"
        app:button2tag="WATCH_NOW"
        app:button2emphasis="true"
        app:emphasisColor="@color/theme_accent_2"
        android:layout_marginBottom="16dp"/>

    <ListView
        android:id="@+id/expense_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

</LinearLayout>
